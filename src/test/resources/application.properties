
spring:
    application:
        name: bankrupt-api
    main:
        allow-bean-definition-overriding: true
    jpa:
        database: postgresql
        ddl-auto: validate

server:
    port: 8100

sync:
    scheduled: -1